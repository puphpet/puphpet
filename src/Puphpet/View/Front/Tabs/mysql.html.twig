<h2 class="page-header">MySQL Settings</h2>
<div class="span3">
    <h5>Step 5: MySQL Setup</h5>
    <ul>
        <li>
            Assign a password to the root user
        </li>
        <li>
            Create a database and user assigned to the database. Currently, to assign the same user to multiple databases,
            you must enter in the same username/password each time.
        </li>
    </ul>

    <br />

    <p class="text-center"><a id="mysqlAddDbuser" class="btn btn-primary" href="#">Add Another DB/User</a></p>
</div>

<fieldset class="span8">
    <legend>Root User Password</legend>
    <div class="control-group">
        <label class="control-label" for="mysql[root]">Root Password</label>

        <div class="controls">
            <input id="mysql[root]" name="mysql[root]" type="text"
                   class="input-xxlarge" placeholder="root_password" value="" />
        </div>
    </div>

    <div class="span7" id="mysql-dbuser-count" rel="1">
        {% include 'Front/Tabs/mysql/dbuser.html.twig' %}
    </div>
</fieldset>
