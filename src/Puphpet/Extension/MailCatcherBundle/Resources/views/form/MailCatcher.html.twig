<section id="mailcatcher">

    <div class="row">
        <div class="col-xs-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">MailCatcher</h3>
                </div>

                <div class="panel-body">
                    <div class="row form-group">
                        <div class="col-xs-12">
                            <label class="checkbox-inline">
                                <input type="checkbox" name="mailcatcher[install]" class="multiselect-grouped-tab"
                                       data-tab-target="mailcatcher-pill"
                                       {% if mailcatcher.install %}checked{% endif %} value="1" /> Install MailCatcher
                            </label>

                            <p class="help-block">
                                MailCatcher runs a super simple SMTP server which catches any message sent to it to
                                display in a web interface.
                                <a href="http://mailcatcher.me/" target="_blank">More information available here</a>
                            </p>
                        </div>
                    </div>

                    <div class="row form-group">
                        <div class="col-md-6">
                            <label for="mailcatcher-settings-http_port">HTTP Port</label>
                            <input type="number" id="mailcatcher-settings-http_port" name="mailcatcher[settings][http_port]"
                                   required placeholder="1080"
                                   pattern="^[1-9][0-9]*$" min="1000" max="20000"
                                   value="{{ mailcatcher.settings.http_port }}" class="form-control" />

                            <p class="help-block">
                                MailCatcher will be available at <code>http://{SERVER_IP_ADDRESS}:{HTTP_PORT}</code>
                            </p>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

</section>
