puphpet.extension.vagrantfile_local.homepage:
    pattern:  /
    defaults: { _controller: PuphpetExtensionVagrantfileLocalBundle:Front:index }

puphpet.extension.vagrantfile_local.synced_folder:
    pattern: /synced-folder
    defaults: { _controller: PuphpetExtensionVagrantfileLocalBundle:Front:syncedFolder }

puphpet.extension.vagrantfile_local.forwarded_port:
    pattern: /forwarded-port
    defaults: { _controller: PuphpetExtensionVagrantfileLocalBundle:Front:forwardedPort }

puphpet.extension.vagrantfile_local.manifest:
    pattern:  /manifest
    defaults: { _controller: PuphpetExtensionVagrantfileLocalBundle:Manifest:index }
