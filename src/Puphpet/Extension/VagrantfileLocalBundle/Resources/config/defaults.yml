vm:
    provider:
        virtualbox: []
    synced_folder:
        -
            source: ./
            target: /var/www
            id: vagrant-root
            nfs: 'false'
            owner: www-data
            group: www-data
    network:
        forwarded_port:
            -
                guest: ~
                host: ~
    provision:
        puppet:
            options:
                - "--verbose"
                - "--hiera_config /vagrant/puphpet/puppet/hiera.yaml"
                - "--parser future"
ssh:
    forward_agent: true
