# Begin redis

if $redis_values == undef {
$redis_values = hiera('redis', false)
}

if $php_values == undef {
$php_values = hiera('php', false)
}

if has_key($redis_values, 'install') and $redis_values['install'] == 1 {
class { 'redis':
}

if is_hash($php_values) and ! defined(Php::Pecl::Module['redis']) {
php_pecl_mod { 'redis': }
}
}
