{% for contributor in contributors %}
    <a href="{{ contributor.html_url }}" class="img-thumbnail" title="Thanks, {{ contributor.login }}!" target="_blank"><img
            src="{{ contributor.avatar_url }}" width="40" height="40" /></a>
{% endfor %}
