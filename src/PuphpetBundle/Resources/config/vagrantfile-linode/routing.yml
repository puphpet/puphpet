puphpet.vagrantfile_linode.homepage:
    pattern:  /
    defaults: { _controller: PuphpetBundle:VagrantfileLinode:index }

puphpet.vagrantfile_linode.machine:
    pattern: /machine
    defaults: { _controller: PuphpetBundle:VagrantfileLinode:machine }

puphpet.vagrantfile_linode.synced_folder:
    pattern: /synced-folder
    defaults: { _controller: PuphpetBundle:VagrantfileLinode:syncedFolder }
